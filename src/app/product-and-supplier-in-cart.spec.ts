import { ProductAndSupplierInCart } from './product-and-supplier-in-cart';

describe('ProductAndSupplierInCart', () => {
  it('should create an instance', () => {
    expect(new ProductAndSupplierInCart()).toBeTruthy();
  });
});
