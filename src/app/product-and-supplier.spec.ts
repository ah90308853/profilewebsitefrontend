import { ProductAndSupplier } from './product-and-supplier';

describe('ProductAndSupplier', () => {
  it('should create an instance', () => {
    expect(new ProductAndSupplier()).toBeTruthy();
  });
});
